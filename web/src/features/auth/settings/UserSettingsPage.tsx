import {Stack, Typography} from "@mui/material";

export function UserSettingsPage() {
    return (
        <Stack sx={{
            p: 8,
        }}>
            <Typography variant={"h3"}>
                Settings screen
            </Typography>
        </Stack>
    );
}
